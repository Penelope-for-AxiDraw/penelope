import styled from 'styled-components';

const Prompt = styled.p`
  font-size: 1.125rem;
  margin: 16px 0 8px;
`;

const SubPrompt = styled.p`
  font-size: 1rem;
  margin: 0;
`;

export { Prompt, SubPrompt };
